# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          touch .env
          echo REACT_APP_FB_ANALYTICS_ENABLED=$FB_ANALYTICS_ENABLED >> .env
          echo REACT_APP_ENVIRONMENT=$ENVIRONMENT >> .env
          echo REACT_APP_FB_API_KEY=$FB_API_KEY >> .env
          echo REACT_APP_FB_AUTH_DOMAIN=$FB_AUTH_DOMAIN >> .env
          echo REACT_APP_FB_DATABASE_URL=$FB_DATABASE_URL >> .env
          echo REACT_APP_FB_PROJECT_ID=$FB_PROJECT_ID >> .env
          echo REACT_APP_FB_APP_ID=$FB_APP_ID >> .env
          echo REACT_APP_FB_MEASUREMENT_ID=$FB_MEASUREMENT_ID >> .env
        env:
          FB_ANALYTICS_ENABLED: true
          ENVIRONMENT: production
          FB_API_KEY: ${{ secrets.FB_API_KEY }}
          FB_AUTH_DOMAIN: ${{ secrets.FB_AUTH_DOMAIN }}
          FB_DATABASE_URL: ${{ secrets.FB_DATABASE_URL }}
          FB_PROJECT_ID: ${{ secrets.FB_PROJECT_ID }}
          FB_APP_ID: ${{ secrets.FB_APP_ID }}
          FB_MEASUREMENT_ID: ${{ secrets.FB_MEASUREMENT_ID }}
      - run: npm install && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_METIS_57C1F }}'
          channelId: live
          projectId: metis-57c1f
